driftfile /var/lib/chrony/drift
ntsdumpdir /var/lib/chrony/nts-dump
rtcfile /var/lib/chrony/rtcfile
# dumpdir /var/lib/chrony
makestep 1 3
cmdport 0
#hwtimestamp

pool pool.ntp.org iburst nts prefer
pool cn.pool.ntp.org iburst nts prefer
server 0.cn.pool.ntp.org nts iburst prefer
server 0.cn.pool.ntp.org nts iburst
server nts.teambelgium.net iburst
pool pool.ntp.org iburst
pool hk.pool.ntp.org iburst
authselectmode ignore
hwclockfile /etc/adjtime
pidfile /run/chrony/chronyd.pid
