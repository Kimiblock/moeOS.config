#!/bin/bash

function filesystemOverride(){
	flatpak override --filesystem="$1"
}

function main(){
	for file in "xdg-config/fontconfig:ro" "xdg-config/gtk-3.0:ro" "xdg-config/gtk-4.0:ro"; do
		filesystemOverride "${file}"
	done
	flatpak override --device=dri
}